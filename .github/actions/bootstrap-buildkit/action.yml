name: 'Bootstrap Buildkit'
description: 'Bootstrap buildkit builders using remote workers or Kubernetes driver'

# This action supports two buildkit driver modes:
#
# 1. Remote Driver (when buildkit_worker_addresses is provided)
#    Uses pre-provisioned remote buildkit workers specified via buildkit_worker_addresses.
#    This is the preferred mode for faster, more reliable builds.
#
# 2. Kubernetes Driver (fallback, when buildkit_worker_addresses is empty)
#    Dynamically creates buildkit pods in Kubernetes. Use this as a fallback when
#    remote buildkit workers are unavailable or unreachable. The Kubernetes driver
#    is slower due to pod startup time but provides on-demand build capacity.
#
# Options:
#   - skip_bootstrap: Set to 'true' to only create the builder without bootstrapping.
#     Useful for cleanup jobs that need to remove the builder but not bootstrap it.

inputs:
  builder_name:
    description: 'Name for the buildx builder'
    required: true
  buildkit_worker_addresses:
    description: 'Comma-separated list of remote buildkit worker addresses. If empty, falls back to Kubernetes driver.'
    required: false
    default: ''
  use_runner_docker_builder:
    description: 'When true and no remote workers are configured, skip Kubernetes builder creation and reuse the runner docker builder.'
    required: false
    default: 'true'


  # Kubernetes driver inputs (used when remote_builder is false)
  ephemeral_storage:
    description: 'Ephemeral storage request for Kubernetes driver'
    required: false
    default: '400Gi'
  namespace:
    description: 'Kubernetes namespace for buildkit pods'
    required: false
    default: 'buildkit'
  arch:
    description: 'Target architecture for fallback kubernetes builder (amd64, arm64, all)'
    required: false
    default: 'all'
  replicas:
    description: 'Number of buildkit replicas'
    required: false
    default: '1'
  requests_cpu:
    description: 'CPU requests for buildkit pods'
    required: false
    default: '12'
  requests_memory:
    description: 'Memory requests for buildkit pods'
    required: false
    default: '26Gi'
  limits_memory:
    description: 'Memory limits for buildkit pods'
    required: false
    default: '29Gi'
  tolerations:
    description: 'Tolerations for buildkit pods'
    required: false
    default: "key=buildkit-fallback-worker,value=true,operator=Equal,effect=NoSchedule"
  http_proxy:
    description: 'HTTP proxy for buildkit kubernetes pods; falls back to runner HTTP_PROXY if unset'
    required: false
    default: ''
  https_proxy:
    description: 'HTTPS proxy for buildkit kubernetes pods; falls back to runner HTTPS_PROXY if unset'
    required: false
    default: ''
  no_proxy:
    description: 'NO_PROXY for buildkit kubernetes pods; falls back to runner NO_PROXY if unset'
    required: false
    default: ''
  dns:
    description: 'DNS nameserver for buildkit kubernetes pods (e.g. 8.8.8.8 or 127.0.0.11); helps resolve external domains'
    required: false
    default: ''
  skip_bootstrap:
    description: 'Skip the bootstrap step (only create the builder)'
    required: false
    default: 'false'

runs:
  using: "composite"
  steps:
    - name: Setup Docker Buildx
      id: setup-buildx
      uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 # v3.11.1
      with:
        driver: docker
        install: true

    - name: Use runner docker builder
      if: inputs.buildkit_worker_addresses == '' && inputs.use_runner_docker_builder == 'true'
      shell: bash
      run: |
        RESOLVED_BUILDER="${{ steps.setup-buildx.outputs.name }}"
        if [ -z "$RESOLVED_BUILDER" ]; then
          RESOLVED_BUILDER="default"
        fi
        echo "Using runner docker builder: ${RESOLVED_BUILDER}"
        docker buildx use "$RESOLVED_BUILDER"
        echo "BUILDX_BUILDER_NAME=${RESOLVED_BUILDER}" >> "$GITHUB_ENV"

    - name: Define remote buildkit builders
      if: inputs.buildkit_worker_addresses != ''
      shell: bash
      run: |
        run_buildx_create() {
          local -a args=("$@")
          local output rc unknown

          while true; do
            output=$(docker buildx create "${args[@]}" 2>&1)
            rc=$?
            if [ $rc -eq 0 ]; then
              echo "$output"
              return 0
            fi

            unknown=$(echo "$output" | sed -n "s/.*unknown flag: \(--[^ ,;]*\).*/\1/p" | head -n 1)
            if [ -z "$unknown" ]; then
              echo "$output" >&2
              return $rc
            fi

            echo "::warning::docker buildx create does not support ${unknown}; retrying without it." >&2

            local -a filtered=()
            local skip_next=false
            local token next
            for ((i=0; i<${#args[@]}; i++)); do
              token="${args[$i]}"
              if $skip_next; then
                skip_next=false
                continue
              fi

              if [[ "$token" == "$unknown" ]]; then
                if (( i + 1 < ${#args[@]} )); then
                  next="${args[$((i+1))]}"
                  if [[ "$next" != --* ]]; then
                    skip_next=true
                  fi
                fi
                continue
              fi

              if [[ "$token" == "${unknown}="* ]]; then
                continue
              fi

              filtered+=("$token")
            done

            if [ ${#filtered[@]} -eq ${#args[@]} ]; then
              echo "$output" >&2
              return $rc
            fi

            args=("${filtered[@]}")
          done
        }

        log_buildx_create_cmd() {
          local -a args=("$@")
          local rendered="docker buildx create"
          local token
          for token in "${args[@]}"; do
            rendered+=" $(printf '%q' "$token")"
          done
          echo "::notice::Executing: ${rendered}"
        }

        ADDRS="${{ inputs.buildkit_worker_addresses }}"
        IFS=',' read -ra ADDR_LIST <<< "$ADDRS"
        SUPPORTS_NAME=false
        if docker buildx create --help | grep -q -- '--name'; then
          SUPPORTS_NAME=true
        fi

        FIRST=true
        RESOLVED_BUILDER="${{ inputs.builder_name }}"
        for addr in "${ADDR_LIST[@]}"; do
          if $FIRST; then
            local_create_output=""
            if $SUPPORTS_NAME; then
              log_buildx_create_cmd --name "${RESOLVED_BUILDER}" --driver remote "$addr"
              local_create_output=$(run_buildx_create --name "${RESOLVED_BUILDER}" --driver remote "$addr")
            else
              # Legacy buildx without --name returns the created builder name on stdout.
              log_buildx_create_cmd --driver remote "$addr"
              local_create_output=$(run_buildx_create --driver remote "$addr")
              RESOLVED_BUILDER=$(echo "$local_create_output" | tr -d '\r' | tail -n 1)
              if [ -z "$RESOLVED_BUILDER" ]; then
                echo "::error::Failed to resolve builder name from buildx create output"
                exit 1
              fi
            fi

            if $SUPPORTS_NAME && ! docker buildx inspect "${RESOLVED_BUILDER}" >/dev/null 2>&1; then
              detected_name=$(echo "$local_create_output" | tr -d '\r' | tail -n 1)
              if [ -n "$detected_name" ]; then
                RESOLVED_BUILDER="$detected_name"
              fi
            fi

            docker buildx use "${RESOLVED_BUILDER}"
            FIRST=false
          else
            if $SUPPORTS_NAME; then
              log_buildx_create_cmd --append --name "${RESOLVED_BUILDER}" --driver remote "$addr"
              run_buildx_create --append --name "${RESOLVED_BUILDER}" --driver remote "$addr" >/dev/null
            else
              docker buildx use "${RESOLVED_BUILDER}"
              log_buildx_create_cmd --append --driver remote "$addr"
              BUILDX_BUILDER="${RESOLVED_BUILDER}" run_buildx_create --append --driver remote "$addr" >/dev/null
            fi
          fi
        done
        echo "BUILDX_BUILDER_NAME=${RESOLVED_BUILDER}" >> "$GITHUB_ENV"

    - name: Create Kubernetes builder
      if: inputs.buildkit_worker_addresses == '' && inputs.use_runner_docker_builder != 'true'
      shell: bash
      run: |
        run_buildx_create() {
          local -a args=("$@")
          local output rc unknown

          while true; do
            output=$(docker buildx create "${args[@]}" 2>&1)
            rc=$?
            if [ $rc -eq 0 ]; then
              echo "$output"
              return 0
            fi

            unknown=$(echo "$output" | sed -n "s/.*unknown flag: \(--[^ ,;]*\).*/\1/p" | head -n 1)
            if [ -z "$unknown" ]; then
              echo "$output" >&2
              return $rc
            fi

            echo "::warning::docker buildx create does not support ${unknown}; retrying without it." >&2

            local -a filtered=()
            local skip_next=false
            local token next
            for ((i=0; i<${#args[@]}; i++)); do
              token="${args[$i]}"
              if $skip_next; then
                skip_next=false
                continue
              fi

              if [[ "$token" == "$unknown" ]]; then
                if (( i + 1 < ${#args[@]} )); then
                  next="${args[$((i+1))]}"
                  if [[ "$next" != --* ]]; then
                    skip_next=true
                  fi
                fi
                continue
              fi

              if [[ "$token" == "${unknown}="* ]]; then
                continue
              fi

              filtered+=("$token")
            done

            if [ ${#filtered[@]} -eq ${#args[@]} ]; then
              echo "$output" >&2
              return $rc
            fi

            args=("${filtered[@]}")
          done
        }

        log_buildx_create_cmd() {
          local -a args=("$@")
          local rendered="docker buildx create"
          local token
          for token in "${args[@]}"; do
            rendered+=" $(printf '%q' "$token")"
          done
          echo "::notice::Executing: ${rendered}"
        }

        TARGET_ARCH="${{ inputs.arch }}"
        SUPPORTS_NAME=false
        if docker buildx create --help | grep -q -- '--name'; then
          SUPPORTS_NAME=true
        fi
        RESOLVED_BUILDER="${{ inputs.builder_name }}"

        case "$TARGET_ARCH" in
          amd64|arm64|all) ;;
          *)
            echo "::error::Invalid arch '$TARGET_ARCH'. Must be amd64, arm64, or all."
            exit 1
            ;;
        esac

        if $SUPPORTS_NAME && docker buildx inspect "${RESOLVED_BUILDER}" > /dev/null 2>&1; then
            # If exit code is 0 (success), print the message
            echo "✅ Builder '${RESOLVED_BUILDER}' already exists. Skipping creation."
            docker buildx use "${RESOLVED_BUILDER}"
        else
          echo "K8s Builder '${RESOLVED_BUILDER}' does not exist. Creating it for arch=$TARGET_ARCH."

          create_node() {
            local mode="$1"      # use|append
            local arch="$2"      # amd64|arm64
            local selector="kubernetes.io/arch=${arch},role=dynamo-builder-fallback"
            local selector_escaped="${selector//,/\\,}"
            local tolerations_escaped="${{ inputs.tolerations }}"
            tolerations_escaped="${tolerations_escaped//,/\\,}"
            local effective_http_proxy="${{ inputs.http_proxy }}"
            local effective_https_proxy="${{ inputs.https_proxy }}"
            local effective_no_proxy="${{ inputs.no_proxy }}"
            if [ -z "$effective_http_proxy" ] && [ -n "${HTTP_PROXY:-}" ]; then
              effective_http_proxy="${HTTP_PROXY}"
            fi
            if [ -z "$effective_https_proxy" ] && [ -n "${HTTPS_PROXY:-}" ]; then
              effective_https_proxy="${HTTPS_PROXY}"
            fi
            if [ -z "$effective_no_proxy" ] && [ -n "${NO_PROXY:-}" ]; then
              effective_no_proxy="${NO_PROXY}"
            fi
            local effective_http_proxy_escaped="${effective_http_proxy//,/\\,}"
            local effective_https_proxy_escaped="${effective_https_proxy//,/\\,}"
            local effective_no_proxy_escaped="${effective_no_proxy//,/\\,}"
            local effective_dns="${{ inputs.dns }}"

            local cmd=()
            if [[ "$mode" == "append" ]]; then
              cmd+=(--append)
            fi

            if $SUPPORTS_NAME; then
              cmd+=(--name "${RESOLVED_BUILDER}")
            fi
            cmd+=(--driver kubernetes --platform="linux/${arch}")
            cmd+=("--driver-opt=requests.ephemeral-storage=${{ inputs.ephemeral_storage }}")
            cmd+=("--driver-opt=namespace=${{ inputs.namespace }}")
            cmd+=("--driver-opt=loadbalance=sticky")
            cmd+=("--driver-opt=replicas=${{ inputs.replicas }}")
            cmd+=("--driver-opt=requests.cpu=${{ inputs.requests_cpu }}")
            cmd+=("--driver-opt=requests.memory=${{ inputs.requests_memory }}")
            cmd+=("--driver-opt=limits.memory=${{ inputs.limits_memory }}")
            cmd+=("--driver-opt=nodeselector=${selector_escaped}")
            cmd+=("--driver-opt=tolerations=${tolerations_escaped}")
            if [ -n "$effective_dns" ]; then
              cmd+=("--driver-opt=dns=${effective_dns}")
            fi
            if [ -n "$effective_http_proxy" ]; then
              cmd+=("--driver-opt=env.http_proxy=${effective_http_proxy_escaped}")
              cmd+=("--driver-opt=env.HTTP_PROXY=${effective_http_proxy_escaped}")
            fi
            if [ -n "$effective_https_proxy" ]; then
              cmd+=("--driver-opt=env.https_proxy=${effective_https_proxy_escaped}")
              cmd+=("--driver-opt=env.HTTPS_PROXY=${effective_https_proxy_escaped}")
            fi
            if [ -n "$effective_no_proxy" ]; then
              cmd+=("--driver-opt=env.no_proxy=${effective_no_proxy_escaped}")
              cmd+=("--driver-opt=env.NO_PROXY=${effective_no_proxy_escaped}")
            fi

            local create_output=""
            local create_ok=false
            if $SUPPORTS_NAME; then
              log_buildx_create_cmd "${cmd[@]}"
              if create_output=$(run_buildx_create "${cmd[@]}" 2>&1); then
                create_ok=true
              else
                create_output=""
              fi
            else
              if [[ "$mode" == "append" ]]; then
                log_buildx_create_cmd "${cmd[@]}"
                if create_output=$(BUILDX_BUILDER="${RESOLVED_BUILDER}" run_buildx_create "${cmd[@]}" 2>&1); then
                  create_ok=true
                else
                  create_output=""
                fi
              else
                log_buildx_create_cmd "${cmd[@]}"
                if create_output=$(run_buildx_create "${cmd[@]}" 2>&1); then
                  create_ok=true
                else
                  create_output=""
                fi
              fi
            fi

            if $create_ok; then
              if ! $SUPPORTS_NAME && [[ "$mode" != "append" ]]; then
                local created_name=""
                created_name=$(echo "$create_output" | tr -d '\r' | tail -n 1)
                if [ -n "$created_name" ]; then
                  RESOLVED_BUILDER="$created_name"
                fi
              fi
              return 0
            fi

            if [ -n "$create_output" ]; then
              echo "$create_output" >&2
            fi
            echo "::notice::Primary node selector failed for ${arch}. Retrying without role selector."
            selector="kubernetes.io/arch=${arch}"
            selector_escaped="${selector//,/\\,}"
            cmd=()
            if [[ "$mode" == "append" ]]; then
              cmd+=(--append)
            fi
            if $SUPPORTS_NAME; then
              cmd+=(--name "${RESOLVED_BUILDER}")
            fi
            cmd+=(--driver kubernetes --platform="linux/${arch}")
            cmd+=("--driver-opt=requests.ephemeral-storage=${{ inputs.ephemeral_storage }}")
            cmd+=("--driver-opt=namespace=${{ inputs.namespace }}")
            cmd+=("--driver-opt=loadbalance=sticky")
            cmd+=("--driver-opt=replicas=${{ inputs.replicas }}")
            cmd+=("--driver-opt=requests.cpu=${{ inputs.requests_cpu }}")
            cmd+=("--driver-opt=requests.memory=${{ inputs.requests_memory }}")
            cmd+=("--driver-opt=limits.memory=${{ inputs.limits_memory }}")
            cmd+=("--driver-opt=nodeselector=${selector_escaped}")
            cmd+=("--driver-opt=tolerations=${tolerations_escaped}")
            if [ -n "$effective_dns" ]; then
              cmd+=("--driver-opt=dns=${effective_dns}")
            fi
            if [ -n "$effective_http_proxy" ]; then
              cmd+=("--driver-opt=env.http_proxy=${effective_http_proxy_escaped}")
              cmd+=("--driver-opt=env.HTTP_PROXY=${effective_http_proxy_escaped}")
            fi
            if [ -n "$effective_https_proxy" ]; then
              cmd+=("--driver-opt=env.https_proxy=${effective_https_proxy_escaped}")
              cmd+=("--driver-opt=env.HTTPS_PROXY=${effective_https_proxy_escaped}")
            fi
            if [ -n "$effective_no_proxy" ]; then
              cmd+=("--driver-opt=env.no_proxy=${effective_no_proxy_escaped}")
              cmd+=("--driver-opt=env.NO_PROXY=${effective_no_proxy_escaped}")
            fi

            local retry_output=""
            local retry_ok=false
            if $SUPPORTS_NAME; then
              log_buildx_create_cmd "${cmd[@]}"
              if retry_output=$(run_buildx_create "${cmd[@]}" 2>&1); then
                retry_ok=true
              fi
            elif [[ "$mode" == "append" ]]; then
              log_buildx_create_cmd "${cmd[@]}"
              if retry_output=$(BUILDX_BUILDER="${RESOLVED_BUILDER}" run_buildx_create "${cmd[@]}" 2>&1); then
                retry_ok=true
              fi
            else
              log_buildx_create_cmd "${cmd[@]}"
              if retry_output=$(run_buildx_create "${cmd[@]}" 2>&1); then
                retry_ok=true
              fi
            fi

            if ! $retry_ok && echo "$retry_output" | grep -Eq "invalid driver option (key|value|operator|effect) for driver kubernetes"; then
              echo "::notice::Buildx rejected tolerations driver-opt fields. Retrying fallback selector without tolerations."
              local -a cmd_no_tolerations=()
              for arg in "${cmd[@]}"; do
                if [[ "$arg" == --driver-opt=tolerations=* ]]; then
                  continue
                fi
                cmd_no_tolerations+=("$arg")
              done

              if $SUPPORTS_NAME; then
                log_buildx_create_cmd "${cmd_no_tolerations[@]}"
                if retry_output=$(run_buildx_create "${cmd_no_tolerations[@]}" 2>&1); then
                  retry_ok=true
                fi
              elif [[ "$mode" == "append" ]]; then
                log_buildx_create_cmd "${cmd_no_tolerations[@]}"
                if retry_output=$(BUILDX_BUILDER="${RESOLVED_BUILDER}" run_buildx_create "${cmd_no_tolerations[@]}" 2>&1); then
                  retry_ok=true
                fi
              else
                log_buildx_create_cmd "${cmd_no_tolerations[@]}"
                if retry_output=$(run_buildx_create "${cmd_no_tolerations[@]}" 2>&1); then
                  retry_ok=true
                fi
              fi
            fi

            if ! $retry_ok; then
              echo "::error::Fallback node selector also failed for ${arch}."
              if [ -n "$retry_output" ]; then
                echo "$retry_output" >&2
              fi
              return 1
            fi

            if ! $SUPPORTS_NAME && [[ "$mode" != "append" ]]; then
              local retry_created_name=""
              retry_created_name=$(echo "$retry_output" | tr -d '\r' | tail -n 1)
              if [ -n "$retry_created_name" ]; then
                RESOLVED_BUILDER="$retry_created_name"
              fi
            fi

            echo "✅ Fallback node selector succeeded for ${arch}."
          }

          if [[ "$TARGET_ARCH" == "amd64" ]]; then
            create_node use amd64
          elif [[ "$TARGET_ARCH" == "arm64" ]]; then
            create_node use arm64
          else
            create_node use amd64
            create_node append arm64
          fi

          docker buildx use "${RESOLVED_BUILDER}"
        fi
        echo "BUILDX_BUILDER_NAME=${RESOLVED_BUILDER}" >> "$GITHUB_ENV"
        sleep 3 # Give the builders some time to be ready

        if [[ "${{ inputs.skip_bootstrap }}" != "true" ]]; then
          echo "::warning::Build is using fallback pod. Please alert the ops team."
          echo "## ⚠️ Fallback Build Warning" >> $GITHUB_STEP_SUMMARY
          echo "This build is running on a **fallback pod**. Please alert the ops team." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
        fi


    - name: Bootstrap buildkit
      if: inputs.skip_bootstrap != 'true'
      shell: bash
      run: |
        BUILDER_NAME="${BUILDX_BUILDER_NAME:-${{ inputs.builder_name }}}"
        echo "Bootstrapping buildkit..."
        for i in 1 2 3; do
          if docker buildx inspect "$BUILDER_NAME" --bootstrap; then
            echo "Bootstrap succeeded on attempt $i"
            break
          fi
          if [ "$i" -eq 3 ]; then
            echo "::error::Bootstrap failed after 3 attempts"
            exit 1
          fi
          echo "::warning::Bootstrap attempt $i failed, retrying in 10s..."
          sleep 10
        done
